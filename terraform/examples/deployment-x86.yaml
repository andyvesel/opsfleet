apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-x86
spec:
  replicas: 2
  selector:
    matchLabels:
      app: app-x86
  template:
    metadata:
      labels:
        app: app-x86
    spec:
      nodeSelector:
        kubernetes.io/arch: amd64
      containers:
        - name: app
          image: nginx:alpine
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
